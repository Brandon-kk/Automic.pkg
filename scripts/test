#!/usr/bin/env bash
# Thin Unix wrapper; Windows / CI can call nvim directly (see Makefile).
set -euo pipefail
root="$(cd "$(dirname "$0")/.." && pwd)"
cd "$root"
exec nvim --headless -u NONE -c "luafile tests/run.lua"
